## Unreachable code explanation

# Issue
Does the compiler catch unreachable code?

# Response
The compiler does not catch it.
Allows both compilation and execution.

# Potential consequences
This would make codes redundant in the eye of the compiler. 
Some code would never execute, no matter what the conditions are.
